{% extends 'base.html.twig' %}
{% block body %}
    list realisation

                {% for realisation in realisations %}

    <div class="container">
        <div class="row">
            <div class="col">
                <div class="card mb-3" style="max-width: 540px;">
                    <div class="row no-gutters">
                        <div class="col-md-4">
                            <img src="..." class="card-img" alt="...">
                        </div>
                        <div class="col-md-8">
                            <div class="card-body">
                                <h5 class="card-title">{{ realisation.titre}}</h5>
                                <p class="card-text">{{ realisation.domaine}}</p>
                                <p class="card-text">{{ realisation.domaine}}</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>
                {% endfor %}
    {% endblock %}

